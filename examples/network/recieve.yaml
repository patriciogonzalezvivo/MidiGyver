out:
    -   csv
    -   midi://virtual

in:
    osc://localhost:8000:
        -   key: 0-127
            name: midi_event
            # shape: |
            #     function() {
            #         // var address = "midi://virtual";
            #         // address += ":" + String(channel) + "/"
            #         // address += status;
            #         return { 
            #             'midi://virtual:1/NOTE_ON': [[key, value]]
            #         };
            #     }