out:
    - midi://OP-Z*
    - csv

in:
    MPK_mini*:
        -   key: 0-120
            channel: 1
            name: pad 
            shape: |
                function() {
                    return { 
                        'midi://OP-Z*:6/NOTE_ON': [[key, value]]
                    };
                }

        -   key: 36
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0) 
                        return {  'midi://OP-Z*:1/NOTE_ON': [[14, value]] };
                    return false;
                }
        
        -   key: 37
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return { 'midi://OP-Z*:1/NOTE_ON': [[15, value]] };
                    return false;
                }

        -   key: 38
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return {  'midi://OP-Z*:2/NOTE_ON': [[14, value]] };
                    return false;
                }

        -   key: 39
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return { 'midi://OP-Z*:2/NOTE_ON': [[15, value]] };
                    return false;
                }
                
        -   key: 40
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)    
                        return { 'midi://OP-Z*:3/NOTE_ON': [[57, value]] };
                    return false;
                }

        -   key: 41
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return { 'midi://OP-Z*:3/NOTE_ON': [[60, value]] };
                    return false;
                }

        -   key: 42
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return { 'midi://OP-Z*:3/NOTE_ON': [[61, value]] };
                    return false;
                }

        -   key: 43
            channel: 10
            name: key
            shape: |
                function() {
                    if (value > 0)
                        return { 'midi://OP-Z*:3/NOTE_ON': [[64, value]] };
                    return false;
                }

